/* =====================================================================
   importReturn(I) (Phase 2 STUB) — the inverse of buildReturn, reads an
   ITR3 return object back into S. Phase 4 implements the readers and
   returns the list of what was read.
   ===================================================================== */
function importReturn(I){return [];}
