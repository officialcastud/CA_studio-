/* =====================================================================
   buildReturn() (Phase 2 STUB) — start from the schema's required-key
   skeleton. Phase 4 fills it via put(...) on the utility's keys.
   `deep` (deep clone) is provided by the shell.
   ===================================================================== */
function buildReturn(){return {ITR:{ITR3:deep(SKEL)}};}
