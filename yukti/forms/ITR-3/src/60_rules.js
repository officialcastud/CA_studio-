/* =====================================================================
   Rules (Phase 2 STUB).
   runRules(I,S_)  — the department's validation rules on the export
                     ([{cat:"A"|"D", n, msg}]). Phase 4 codes the rules.
   auditRules(b)   — the form's own arithmetic audit (messages). Phase 4.
   ===================================================================== */
function runRules(I,S_){return [];}
function auditRules(b){return [];}
