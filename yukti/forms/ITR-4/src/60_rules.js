/* =====================================================================
   ITR-4 (Sugam) · AY 2026-27 — the department's validation rules (Phase 6).
   Stubs for Phase 2: 90_wiring only defines runRules/auditRules if they are
   not already functions, so these no-op definitions keep the shell contract
   satisfied until the real rules (books/ITR-4/rules.json) are encoded.
   ===================================================================== */
function runRules(I,S_){ return []; }
function auditRules(b){ return []; }
